import { Step4 } from "@src/sections/signup";
import React from 'react';

const Waiting = () => {
    return (
        <div id="sign_up">
            <Step4/>
        </div>
    );
};

export default Waiting;